{
  "name": "autosell-website",
  "private": true,
  "description": "Monorepo root; Next.js app lives in web/. Run npm commands here or cd web.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web"
  }
}
